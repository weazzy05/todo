import 'package:flutter/material.dart';

class TodoLightColors {
  static const supportSeparator = Color(0x33000000);
  static const supportOverlay = Color.fromRGBO(0, 0, 0, 0.06);

  static const labelPrimary = Color(0xFF000000);
  static const labelSecondary = Color.fromRGBO(0, 0, 0, 0.6);
  static const labelTertiary = Color.fromRGBO(0, 0, 0, 0.3);
  static const labelDisable = Color.fromRGBO(0, 0, 0, 0.15);

  static const red = Color(0xFFFF3B30);
  static const green = Color(0xFF34C759);
  static const blue = Color(0xFF007AFF);
  static const gray = Color(0xFF8E8E93);
  static const lightGray = Color(0xFFD1D1D6);
  static const white = Color(0xFFFFFFFF);

  static const backPrimary = Color(0xFFF7F6F2);
  static const backSecondary = Color(0xFFFFFFFF);
  static const backElevated = Color(0xFFFFFFFF);
}

class TodoDarkColors {
  static const supportSeparator = Color.fromRGBO(255, 255, 255, 0.2);
  static const supportOverlay = Color.fromRGBO(0, 0, 0, 0.32);

  static const labelPrimary = Color(0xFFFFFFFF);
  static const labelSecondary = Color.fromRGBO(255, 255, 255, 0.6);
  static const labelTertiary = Color.fromRGBO(255, 255, 255, 0.4);
  static const labelDisable = Color.fromRGBO(255, 255, 255, 0.15);

  static const red = Color(0xFFFF453A);
  static const green = Color(0xFF32D74B);
  static const blue = Color(0xFF0A84FF);
  static const gray = Color(0xFF8E8E93);
  static const lightGray = Color(0xFF48484A);
  static const white = Color(0xFFFFFFFF);

  static const backPrimary = Color(0xFF161618);
  static const backSecondary = Color(0xFF252528);
  static const backElevated = Color(0xFF3C3C3F);
}
